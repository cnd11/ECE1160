#!/usr/bin/perl - w

my @ar = {"A", "A#", "B", "C", "C#", "D", "D#", "E","F","F#", "G", "G#"};
my $count = 0;

for my $i (21..108) 
{
    my $note = pop @ar;
    
    if ($note =~/C/){ $count++;print "C " . count."\n";  }

    push(@ar, $note);

}
